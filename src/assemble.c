#include <stdlib.h>
#include <assert.h>
#include "combined_utils.h"
#include "assemble_utils.h"

// loads the instruction lines into an array of strings (given)
int loadFromFile(char **destArray, char *filepath) {
  return EXIT_FAILURE;
}

int main(int argc, char **argv) {
  assert (argc == 3);
  
  return EXIT_SUCCESS;
}
